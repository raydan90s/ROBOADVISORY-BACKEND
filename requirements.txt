# --- API ---
fastapi==0.115.6
uvicorn[standard]==0.34.0
pydantic==2.10.4
pydantic-settings==2.7.0

# --- Base de datos: Postgres directo (Session pooler de Supabase) ---
psycopg[binary]>=3.2.10
psycopg_pool>=3.2.4

# --- Auth: hashing de contraseñas y JWT ---
bcrypt==4.2.1
pyjwt==2.10.1
python-multipart==0.0.20

# --- Entorno ---
python-dotenv==1.0.1

# --- Tests ---
pytest==8.3.4
pytest-asyncio==0.25.0
httpx==0.28.1

# --- IA: Agente / Máquina de estados ---
langgraph==0.2.60
langchain-core==0.3.28
langchain-google-genai==2.0.7
google-generativeai==0.8.3

# Proveedores adicionales de IA (intercambiables desde AI_PROVIDER en el .env).
# Están aquí para que cambiar de modelo sea solo configuración; si no los usas, no
# molestan (se importan de forma perezosa en llm_provider.py).
langchain-openai==0.2.14
langchain-anthropic==0.3.0

# --- Mercados externos: wrapper cacheado de Alpha Vantage ---
cachetools==5.5.0
